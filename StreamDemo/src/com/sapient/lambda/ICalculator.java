package com.sapient.lambda;

//@FunctionalInterface
public interface ICalculator {
	int doCalc(int a, int b);
	// only one method @FunctionalInterface you need
	// only one function so we need to comment this
	// int doSum(int a,int b);

}
